<%- include('../layout/header') %>

    <main class="container my-5">
        <h1 class="mb-4">
            <%= t('legal_notice.title') %>
        </h1>
        <section class="card p-4 shadow-sm">
            <p>
                <strong>
                    <%= t('legal_notice.section1_title') %>
                </strong>
                <%= t('legal_notice.section1_text') %>
            </p>

            <p>
                <strong>
                    <%= t('legal_notice.section2_title') %>
                </strong>
                <%= t('legal_notice.section2_text') %>
            </p>

            <p>
                <strong>
                    <%= t('legal_notice.section3_title') %>
                </strong>
                <%= t('legal_notice.section3_text') %>
            </p>
        </section>
    </main>

    <%- include('../layout/footer') %>