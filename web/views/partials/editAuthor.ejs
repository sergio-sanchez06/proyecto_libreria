<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Autor - <%= author.name %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>

    <%- include('../layout/header') %>

        <div class="container py-5">
            <h1 class="mb-4">Editar Autor</h1>

            <form action="/author/update/<%= author.id %>" method="POST" enctype="multipart/form-data">
                <input type="hidden" name="_method" value="PUT">

                <div class="mb-3">
                    <label class="form-label">Nombre</label>
                    <input type="text" name="name" value="<%= author.name %>" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">País</label>
                    <input type="text" name="country" value="<%= author.country %>" class="form-control">
                </div>

                <div>

                    <label>Biografia</label>
                    <textarea name="biography" id="biography" cols="30" rows="10" class="form-control"
                        value="<%= author.biography %>"></textarea>

                </div>

                <div class="mb-3">
                    <label class="form-label">Foto (URL)</label>
                    <input type="file" name="author_photo" value="<%= author.photo_url %>" class="form-control">
                </div>

                <button type="submit" class="btn btn-success">Guardar cambios</button>
                <a href="/author/<%= author.id %>" class="btn btn-secondary">Cancelar</a>
            </form>
        </div>
</body>

<%- include('../layout/footer') %>

</html>