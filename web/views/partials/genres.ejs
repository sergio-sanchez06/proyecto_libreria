<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Explorar Géneros Literarios</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/main.css">
    <style>
        .genre-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border: none;
            background: #f8f9fa;
        }

        .genre-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1) !important;
            background: #ffffff;
        }

        .icon-box {
            width: 50px;
            height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 12px;
            background-color: #e7f1ff;
            color: #0d6efd;
            margin-bottom: 1rem;
        }
    </style>
</head>

<body class="bg-light">

    <%- include('../layout/header') %>

        <div class="container py-5">
            <header class="text-center mb-5">
                <h1 class="display-4 fw-bold">Géneros Literarios</h1>
                <p class="text-muted">Explora nuestra colección clasificada por temáticas</p>
                <hr class="w-25 mx-auto border-primary border-2 opacity-75">
            </header>

            <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-4">
                <% genres.forEach(genre=> { %>
                    <div class="col">
                        <div class="card h-100 shadow-sm genre-card">
                            <div class="card-body d-flex flex-column align-items-center text-center p-4">
                                <div class="icon-box">
                                    <i class="bi bi-book-half fs-4"></i>
                                </div>

                                <h5 class="card-title fw-bold mb-3">
                                    <%= genre.name %>
                                </h5>

                                <p class="card-text small text-muted mb-4">
                                    Descubre los mejores títulos de <%= genre.name.toLowerCase() %>.
                                </p>

                                <a href="/genres/<%= genre.name %>"
                                    class="btn btn-outline-primary btn-sm mt-auto w-100 rounded-pill">
                                    Explorar catálogo
                                </a>
                            </div>
                        </div>
                    </div>
                    <% }) %>
            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <%- include('../layout/footer') %>

</body>

</html>