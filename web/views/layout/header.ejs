<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'LibreShop' %> | Sistema de Librería</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <link rel="stylesheet" href="/css/main.css?v=2">
    <link rel="stylesheet" href="/css/forms.css">
    
    <% if (typeof user !== 'undefined' && user && user.rol === 'administrador') { %>
        <link rel="stylesheet" href="/css/admin.css">
    <% } %>

    <style>
        .nav-cart {
            position: relative;
            padding-right: 15px;
        }
        #cart-count {
            font-size: 0.7rem;
            vertical-align: top;
            margin-left: -5px;
        }
    </style>
</head>
<body>
    <header class="main-header bg-dark py-3">
        <div class="container d-flex justify-content-between align-items-center">
            <h1 class="logo m-0">
                <a href="/" class="text-white text-decoration-none fw-bold">BookShop</a>
            </h1>
            
            <nav class="main-nav d-flex align-items-center gap-3">
                <a href="/" class="text-white text-decoration-none">Inventario</a>

                <a href="/carrito" class="text-white text-decoration-none nav-cart">
                    <i class="bi bi-cart3"></i> Carrito 
                    <span id="cart-count" class="badge rounded-pill bg-danger" style="display: none;">0</span>
                </a>

                <% if (typeof user !== 'undefined' && user) { %>
                    <span class="text-secondary">|</span>
                    <a href="/perfil" class="text-white text-decoration-none">Perfil</a>
                    <a href="/historial" class="text-white text-decoration-none">Mis Compras</a>
                    
                    <% if (user.rol === 'administrador') { %>
                        <span class="text-secondary">|</span>
                        <a href="/admin" class="text-warning text-decoration-none">Panel Admin</a>
                        <a href="/admin/libros/new" class="text-warning text-decoration-none">Añadir Libro</a>
                    <% } %>
                    
                    <span class="text-secondary">|</span>
                    <a href="/logout" class="btn btn-outline-light btn-sm">
                        Cerrar Sesión (<%= user.nombre || user.name || 'Usuario' %>)
                    </a>

                <% } else { %>
                    <span class="text-secondary">|</span>
                    <a href="/login" class="text-white text-decoration-none">Iniciar Sesión</a>
                    <a href="/register" class="btn btn-primary btn-sm">Registrarse</a>
                <% } %>
            </nav>
        </div>
    </header>
    
    <main class="content-wrapper container mt-4">

<script type="module" src="/js/cart.mjs"></script>