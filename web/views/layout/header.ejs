<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Librería Online</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- <link rel="stylesheet" href="/css/style.css"> si tienes CSS personalizado -->
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>
    <header class="bg-primary text-white py-3 shadow-sm">
        <div class="container">
            <div class="d-flex justify-content-between align-items-center">
                <h1 class="h3 mb-0">
                    <a href="/" class="text-white text-decoration-none">Librería Online</a>
                </h1>

                <nav class="d-flex align-items-center gap-4">
                    <a href="/" class="text-white">Inicio</a>
                    <a href="/books/showAllBooks" class="text-white">Libros</a>
                    <a href="/authors/showAllAuthors" class="text-white">Autores</a>
                    <a href="/publisher/showAllPublishers" class="text-white">Editoriales</a>
                    <a href="/genres" class="text-white">Géneros</a>
                    <a href="/cart/view" class="text-white">Carrito</a>

                    <% if (user) { %>
                        <span class="text-white">Hola, <%= user.name || user.email %></span>
                        <a href="/user/profile" class="btn btn-outline-light btn-sm">Perfil</a>
                        <a href="/logout" class="btn btn-outline-light btn-sm">Cerrar sesión</a>
                        <% if(user.role==='ADMIN' ) { %>
                            <a href="/admin/dashboard" class="btn btn-outline-light btn-sm">Consola de
                                administración</a>
                            <% } %>
                                <% } else { %>
                                    <a href="/login" class="btn btn-outline-light btn-sm">Iniciar sesión</a>
                                    <a href="/register" class="btn btn-light btn-sm">Registrarse</a>
                                    <% } %>
                </nav>
            </div>
        </div>
    </header>

    <main class="min-vh-100">