<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %> | Sistema de Librería</title>

    <link rel="stylesheet" href="/css/main.css" />
    <link rel="stylesheet" href="/css/forms.css" />
    <link rel="stylesheet" href="/css/detalle.css" />

    <% if (user && user.rol === 'administrador') { %>
    <link rel="stylesheet" href="/css/admin.css" />
    <% } %>
  </head>
  <body>
    <header class="main-header">
      <div class="container">
        <h1 class="logo">BookShop</h1>

        <nav class="main-nav">
          <a href="/">Inventario</a>

          <% if (user) { %>
          <a href="/users/perfil">Perfil</a>
          <a href="/users/historial">Mis Compras</a>
          <a href="/orders/cart">Carrito</a>

          <% if (user.rol === 'administrador') { %>
          <span class="separator">|</span>
          <a href="/admin" class="nav-admin">Panel Admin</a>
          <% } %>

          <span class="separator">|</span>
          <a href="/auth/logout" class="nav-auth"
            >Cerrar Sesión (<%= user.name || user.nombre %>)</a
          >

          <% } else { %>
          <a href="/login" class="nav-auth">Iniciar Sesión</a>
          <a href="/auth/register" class="nav-auth">Registrarse</a>
          <% } %>
        </nav>
      </div>
    </header>

    <main class="content-wrapper"></main>
  </body>
</html>
